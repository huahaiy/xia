;; List all scheduled tasks.
;; Import with: /import-tool resources/tools/schedule-list.edn

{:id          :schedule-list
 :name        "schedule-list"
 :description "List all scheduled tasks with their status, schedule spec, and next run time."
 :parameters  {"type" "object" "properties" {}}
 :handler     "(fn [_] (xia.schedule/list-schedules))"}
